doctype html
head
    title TS Quiz
    meta(charset='UTF-8')
    style
        include main.css

.topnav
    a(href='./quizzes') « Quizzes
h1 Change Password
form(action="/change_pass" method="POST")
    input#password_box(type="password", placeholder="new password", name="new_password")
    input#password_box2(type="password", placeholder="new password (again)", name="new_password2")
    input#submit_button(type="submit", name="submit", value="Change Password")
    input(type="hidden" name="_csrf" value=csrfToken)
