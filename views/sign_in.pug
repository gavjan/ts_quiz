doctype html
head
    title TS Quiz
    meta(charset='UTF-8')
    style
        include main.css
h1 Login to Quizes
form(action="/sign_in" method="POST")
    input#username_box(type="text", placeholder="username", name="username")
    input#password_box(type="password", placeholder="password", name="password")
    input#submit_button(type="submit", name="submit", value="Sign in")
    input(type="hidden" name="_csrf" value=csrfToken)
